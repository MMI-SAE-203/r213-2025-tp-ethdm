---
import { getOffresBySurface } from "../../../js/backend.mjs";
import Card from '../../../components/Card.astro';
import Base from '../../../Layouts/Base.astro';
const { surface } = Astro.params;

const offres = await getOffresBySurface(surface);
console.log(offres);
---

<Base>
<h1 class="font-bold mt-10 text-blue-900">Nos offres de {surface}m² et plus</h1>
    <div class="flex justify-center text-4xl mt-10 space-x-5">
    {
        offres.map((offre) => <Card {...offre} />)
    }
    </div>
</Base>